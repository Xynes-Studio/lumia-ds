import * as React from 'react';
import { cn } from '@lumia/ui';

export interface {{pascalCase name}}Props extends React.HTMLAttributes<HTMLDivElement> {
  // Add custom props here
}

export const {{pascalCase name}} = React.forwardRef<HTMLDivElement, {{pascalCase name}}Props>(
  ({ className, ...props }, ref) => {
    return (
      <div
        ref={ref}
        className={cn('{{kebabCase name}}-root', className)}
        {...props}
      />
    );
  }
);

{{pascalCase name}}.displayName = '{{pascalCase name}}';
