/**
 * Table nodes are provided by @lexical/table.
 * This module re-exports them for convenience and type-safety.
 */
export {
  TableNode,
  TableRowNode,
  TableCellNode,
  $createTableNode,
  $createTableRowNode,
  $createTableCellNode,
  $isTableNode,
  $isTableRowNode,
  $isTableCellNode,
} from '@lexical/table';
