/**
 * Error widget components for graceful degradation.
 * Display user-friendly error states instead of white screens when configs are invalid.
 */

export { PageErrorWidget, type PageErrorWidgetProps } from './PageErrorWidget';
export {
  BlockErrorWidget,
  type BlockErrorWidgetProps,
} from './BlockErrorWidget';
